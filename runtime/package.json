{
  "name": "@clawmarket/runtime",
  "version": "0.1.0",
  "description": "ClawMarket agent runtime: AXL P2P client + bounty loop + 0G Compute brain.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist", "src"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "dev": "tsx src/run.ts"
  },
  "dependencies": {
    "@clawmarket/sdk": "file:../sdk",
    "viem": "^2.21.0",
    "fastify": "^5.0.0"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "@types/node": "^22.0.0"
  }
}
